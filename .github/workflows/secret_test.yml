name: Secret Masking Test

on: workflow_dispatch

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Attempt to print secret
        run: |
          echo "Testing secret visibility..."
          echo "The sensitive token is: ${{ secrets.MY_SENSITIVE_TOKEN }}"

